# Supabase (dein Projekt — Secrets nur in Vercel / lokal .env.local)
NEXT_PUBLIC_SUPABASE_URL=https://lkqopssstvtekneycpgh.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-or-publishable-key

# Server-only: bypasses RLS for conversion ingestion & admin jobs
SUPABASE_SERVICE_ROLE_KEY=your-service-role-or-secret-key

# Optional: kanonische App-URL (Custom Domain auf Vercel)
# NEXT_PUBLIC_APP_URL=https://analytics.eure-hotelgruppe.de
# Comma-separated hotel origins, or * (reflects request Origin + Allow-Credentials)
# HGAE_CORS_ORIGINS=*
